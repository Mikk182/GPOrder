@model IEnumerable<GPOrder.Models.Bill>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CreateUser.UiCulture)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GroupedOrder.CreateUser_Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GroupedOrder.Id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CreateUser.UiCulture)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GroupedOrder.CreateUser_Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GroupedOrder.Id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
